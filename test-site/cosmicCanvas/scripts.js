// Function Definition: This code runs only when it is called.
function displayAccessCode(code) {
    // This is a parameter (code) - data passed into the function
    alert("Your temporary access code is: " + code);
}

// Function Call: This command makes the code inside the function run now,
// passing "ZX-99" as the parameter.
displayAccessCode("ZX-99");