/* Interactive Web Behavior
   Name: __________________
*/

document.addEventListener("DOMContentLoaded", function() {
    
    // --- PART A: MOOD SWINGS (Image Rollover) ---
    // 1. Select the image (#moodImage)
    
    // 2. Define URLs for "Normal" and "Happy"
    const normalSrc = "https://api.dicebear.com/7.x/bottts/svg?seed=Normal&backgroundColor=c0aede";
    const happySrc = "https://api.dicebear.com/7.x/bottts/svg?seed=Happy&backgroundColor=ffd5dc";

    // 3. Write helper functions (makeHappy, makeNormal)

    // 4. Attach Event Listeners (mouseover, mouseout)


    // --- PART B: FACT TOGGLE ---
    // 1. Select button and fact paragraph
    
    // 2. Write the toggle function
    //    - Toggle the "hidden" class
    //    - Check classList to update button text ("Show Fact" vs "Hide Fact")

    // 3. Attach Click Listener


    // --- PART C: FORM VALIDATION ---
    // 1. Select form, input, and error span
    
    // 2. Attach SUBMIT listener to the FORM
    //    - Prevent default browser action
    //    - Check if value is empty or has numbers
    //    - Update error message and class if invalid
    //    - Alert "Success" if valid

});