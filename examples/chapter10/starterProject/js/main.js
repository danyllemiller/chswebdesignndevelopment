/* DOM Detectives - Main Mission File
   Agent Name: ____________________
*/

document.addEventListener("DOMContentLoaded", function() {
    console.log("DOM fully loaded and parsed. Investigation begins.");

    // --- STEP 1: SELECTION ---
    // Select your elements using document.querySelector() and store them in const variables.
    
    // 1. The Pet Image
    
    
    // 2. The Pet Name (h2)
    
    
    // 3. The Hidden Fact (p with class hidden-fact)
    
    
    // 4. The Adopt Button


    // --- STEP 2: INTERROGATION ---
    // Console log your variables to make sure you found them.
    // Example: console.log(petName);


    // --- STEP 3: MANIPULATION ---
    // Change the DOM!
    
    // 1. Change the pet's name to something new
    
    
    // 2. Change the image src to a Corgi (URL in instructions)
    
    
    // 3. Remove the 'hidden' class from the fact paragraph
    // Hint: use .classList.remove("class-name")


    // --- STEP 4: INDEPENDENT PRACTICE ---
    // Make one extra change of your choice below:
    

});